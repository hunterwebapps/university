import { } from '@store/request';

// Types

// Thunks

export const _ = () => dispatch => {

};

// Pure Actions

// Reducers

const initialState = {

};

export default (state = initialState, { type, payload  }) => {
  switch (type) {
    default:
      return state;
  }
};

// Selectors
